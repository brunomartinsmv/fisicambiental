# notebooks

**PT**
Os notebooks sao o guia principal. Eles explicam motivacao, metodos e interpretacao dos resultados. Sempre que possivel, use as funcoes do pacote `scripts` para evitar duplicacao de logica.

**EN**
Notebooks are the main guide. They explain motivation, methods, and interpretation. Whenever possible, call functions from `scripts` to avoid duplicated logic.

## Como usar / How to use
Abra e execute os notebooks na ordem indicada pelos titulos, validando os outputs em `outputs/`.
